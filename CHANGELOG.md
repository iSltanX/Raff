# سجل التغييرات

جميع التغييرات المهمة في «رفّ» موثّقة هنا.

## v4.2.1 — 2026-08-13

إصلاح خط أنابيب الإصدار فقط — v4.2.0 لم تُنشر علنًا قط بسبب هذا الخلل، فهذا
أول إصدار فعلي يحمل تحسينات v4.2.0. لا تغيير سلوكي عن v4.2.0.

- **إصلاح خط أنابيب النشر على GitHub Actions**: خطوة تصحيح أيقونة التطبيق
  الجديدة (المُضافة في v4.2.0) كانت تبحث عن حزمة `Raff.app` في المسار الذي
  يبنيه `tauri build` بلا خيار `--target`، بينما `tauri-action` في هذا
  المستودع يُشغَّل بـ `--target aarch64-apple-darwin` — وCargo يضع مخرجات أي
  بناء محدَّد بمعمارية داخل `target/<المعمارية>/release/` دائمًا، حتى إن كانت
  المعمارية نفس معمارية الجهاز. النتيجة: فشلت الخطوة فورًا في أول تشغيل حقيقي
  لها، قبل أن تلمس الأيقونة. صار السكربت يتحقق من المسارين معًا الآن.

## v4.2.0 — 2026-08-13

يغيّر طريقة استخدام رفّ اليومية: اختيار عنصر أصبح إجراءً واحدًا كاملًا، مع
إصلاح استجابة اللوحة عند أول فتح، وأيقونة تطبيق ثابتة فعليًا على macOS الحديث.

- **اختيار العنصر إجراء واحد الآن**: نقرة واحدة (أو ⏎) تنسخ العنصر إلى
  الحافظة، تُغلق رفّ، تعيد تفعيل التطبيق الذي كنت فيه، وتلصق العنصر هناك
  مباشرة — والعنصر نفسه يبقى في الحافظة بعدها، فيمكن لصقه مجددًا بـ ⌘V. سابقًا
  كانت النقرة تُحدِّد فقط، ويحتاج اللصق نقرتين أو ⏎ منفصلة. النقر المزدوج
  أُزيل لأن الإجراء الآن يتم بنقرة واحدة (نقرتان كانتا ستُلصقان مرتين).
  ⌥ مع النقر (أو ⌥⏎) يبقيان طريقة اللصق كنص عادي؛ ⌘C يبقى للنسخ دون لصق.
- **رجوع واضح حين لا يتوفر إذن تسهيل الوصول**: العنصر يُنسخ دائمًا بغض النظر
  عن الإذن. عند تعذّر اللصق التلقائي تحديدًا، تعرض رسالة رفّ زر «منح الإذن»
  المباشر إلى جانب توجيه اللصق اليدوي بـ ⌘V، بدل رسالة عامة لا تشرح السبب.
- **إصلاح استجابة أول فتح للوحة**: أيقونة شريط القائمة كانت تظهر بسرعة، لكن
  النقرات الأولى بعد التشغيل كانت لا تُحدث شيئًا لعشرات الثواني أحيانًا.
  السبب: البحث عن أيقونة تطبيق كل مصدر مختلف في السجل كان يُنفَّذ على مسار
  الواجهة الرئيسي نفسه — وهو استعلام نظام متزامن يُحتمل أن يستغرق وقتًا لكل
  تطبيق غير مخزَّن مسبقًا. صار الآن يُنفَّذ في الخلفية دون حجب الواجهة، وصور
  السجل المصغَّرة تُحمَّل تدريجيًا حين تدخل نطاق العرض فقط بدل تحميلها كلها
  فور فتح اللوحة.
- **أيقونة التطبيق الفاتحة ثابتة فعليًا في الوضعين**: على أحدث إصدارات macOS
  كانت أيقونة رفّ تظهر داكنة في الوضع الداكن رغم أن الملف المشحون كان يحوي
  التصميم الفاتح الصحيح فقط — النظام يعيد رسم خلفية الأيقونات القديمة بلون
  داكن تلقائيًا. عولج الآن عبر ربط صحيح بنظام الأيقونة الحديث (Icon Composer)
  يُثبِّت المظهر الفاتح صراحة في كلا وضعي النظام — لا تبديل، ولا أيقونة داكنة
  نشطة. صُحِّح أيضًا خط أنابيب الإصدار نفسه ليبني حزمة DMG وأرشيف التحديث من
  التطبيق المصحَّح ذاته، بدل بنائهما قبل التصحيح كما كان يحدث سابقًا.
- تسلسل هرمي أوضح للألوان، إجراءات صف موحّدة، وتزامن كامل مع نظام Figma
  المعتمد — استمرار للصقل البصري في v4.1.0/v4.1.1.

لا تغيير في صيغة بيانات الحافظة أو مكانها، ولا في آلية الالتقاط، ولا في سياسة
أيقونة التطبيق: يبقى رفّ يشحن أيقونة فاتحة واحدة ثابتة بصرف النظر عن مظهر
macOS، بينما تتبع واجهته الداخلية إعداد المظهر أو النظام كما كان (تلقائي/فاتح/داكن).

## v4.1.1 — 2026-08-13

إصلاح واحد على v4.1.0: نقرة أيقونة شريط القائمة كانت تحتاج أحيانًا نقرتين.

- **إصلاح نقرة أيقونة شريط القائمة**: عند إغلاق اللوحة بالنقر على أيقونة رفّ
  في شريط القائمة بينما كانت مفتوحة، كانت اللوحة تفقد تركيزها (بسبب النقرة
  نفسها) فتُغلَق، ثم يعيد معالج النقرة فتحها فورًا لأنه يفحص حالة الظهور بعد
  إغلاقها بلحظة — فتبدو النقرة وكأنها لم تُحدث شيئًا، وتحتاج نقرة ثانية
  لرؤية أي أثر فعلي. أصبح معالج النقرة الآن يتجاهل إعادة الفتح إن كانت اللوحة
  قد أُغلقت للتو بفقدان التركيز، فتُغلَق اللوحة من أول نقرة كما يُتوقَّع.
- تأكيد إضافي أن أيقونة التطبيق المشحونة في هذا الإصدار هي النسخة الفاتحة
  المعتمدة نفسها بلا تغيير — لا أيقونة داكنة، ولا خلل شفافية؛ أُعيد تسجيل
  الحزمة مع LaunchServices لضمان عدم بقاء أي أيقونة مؤقتة من بناء سابق في
  الذاكرة المؤقتة لـ Finder.

## v4.1.0 — 2026-08-13

صقل بصري شامل على واجهة v4.0.0، مبني بالكامل على نظام Figma المعتمد نفسه، دون
تغيير في البيانات المحفوظة أو آلية الالتقاط أو الاختصارات.

- **تباين أوضح بين الصف والخلفية**: عُمّقت خلفية اللوحة ورُفعت خلفية كل صف
  خطوة واحدة في كلا الوضعين، فصار كل صف يقرأ كبطاقة قائمة فوق النافذة بدل أن
  يذوب فيها — بنفس درجة التباين في الفاتح والداكن.
- **مستويات نص أكثر اتساقًا**: أعيد ضبط التدرّج بين النص الأساسي والثانوي
  والثالث ليحافظ على النسبة نفسها بين الوضعين، مع استعادة تباين اللون الأخضر
  (نجاح) لمعيار AA على الخلفية الأعمق.
- **إجراءات الصف كطبقة واحدة عائمة**: أزرار التثبيت والحذف تظهران الآن كمجموعة
  واحدة متماسكة عند المرور أو التحديد أو التنقل بالكيبورد، بدل ظهور كل زر
  بمفرده — سلوك أوضح وأكثر انسجامًا مع البقية.
- **العناصر المثبّتة تحمل علامة مميزة**: صف العنصر المثبّت يحمل الآن حافة
  بلون الهوية وعلامة دبوس صغيرة في راحته، بدل زر إلغاء تثبيت ظاهر بشكل دائم.
- **أيقونات مصدر أوضح**: أيقونة التطبيق المصدر بجانب كل عنصر أكبر قليلاً
  (24 بدل 20 بكسل) فلا تبدو ضبابية على الشاشات عالية الدقة.
- **حالة التحميل أدق**: الهيكل الوهمي أثناء التحميل لم يعد يحجز مساحة لزر
  إجراء غير موجود أصلاً في الصف الحقيقي.
- **رسالة «الرفّ فارغ» بصياغتها المعتمدة**: أعيدت الصياغة إلى النص المعتمد في
  Figma بعد أن انحرفت عنه واجهة v4.0.0.
- **رسائل التنبيه (Toast) أخف وأصدق لهويتها**: تحمل لون الحالة (نجاح، خطأ،
  تثبيت) بحدّ رفيع بدل خلفية ملوّنة كاملة؛ عولجت مشكلة كانت تُظهر كبسولة داكنة
  معتمة عند الحذف تحديدًا.
- **تسلسل هرمي أوضح في الإعدادات وعن رفّ**: عنوان كل صفحة إعدادات صار بلون
  النص الأساسي بدل لون الهوية (الذي بات محجوزًا للعناصر النشطة فعليًا فقط)،
  أزيل خط فاصل مكرر أعلى شريط التبويبات، ورقم الإصدار في نافذة «عن رفّ» صار
  أهدأ بصريًا من سطر الاعتماد أعلاه.
- **أيقونة التطبيق تُصدَّر شفافة فعليًا**: عولج خلل في تصدير أصول الأيقونة كان
  يخلط خلفية معتمة خفيفة في زوايا الأيقونة المستديرة؛ محكوم الآن باختبار آلي
  يمنع تكراره.
- **رسوم حالة «الرفّ فارغ» منقّحة**: تفاصيل خطوط الرسم التوضيحي الثلاثة
  مصقولة لتطابق الإصدار المعتمد في Figma.
- **مزامنة كاملة مع Figma**: أُصلح رقم إصدار قديم (v1.0.0) كان باقيًا في أصلين
  من أصول صفحة «GitHub & README»، وصودق على كل الأصول الحية مقابل النظام
  المعتمد قبل التصدير.
- **تحديث شامل لعرض المستودع على GitHub**: صفحة README أعيد بناؤها بالكامل
  حول لقطات ورسوم مصدَّرة مباشرة من صفحة «GitHub & README» المعتمدة في Figma —
  هوية، شرح المنتج، الواجهة في الوضعين، البحث، التثبيت، دعم الصور، شريط
  القوائم، الإعدادات، والتثبيت.

هذا الإصدار لا يغيّر صيغة بيانات الحافظة، ولا مكان تخزينها، ولا الاختصارات
الحالية، ولا سياسة أيقونة التطبيق: يبقى رفّ يشحن أيقونة فاتحة واحدة ثابتة بصرف
النظر عن مظهر macOS، بينما تتبع واجهته الداخلية إعداد المظهر أو النظام كما كان.

## v4.0.0 — 2026-08-13

رفّ بحلّته الجديدة كليًا: واجهة أعيد بناؤها من الألف إلى الياء، وطريقة أسرع
للوصول إلى ما نسخته.

- **نقرة واحدة تفتح رفّ**: الضغط على أيقونة رفّ في شريط القائمة يفتح اللوحة
  مباشرة. أما النقر بالزر الأيمن فيعرض قائمة مختصرة: الإعدادات، التحقق من
  التحديثات، عن رفّ، وإنهاء.
- **لوحة جديدة بالكامل**: تصميم أهدأ وأوضح، مع وقت النسخ إلى جانب كل عنصر
  واسم التطبيق الذي جاء منه وأيقونته، ومعاينة أنيقة للصور.
- **تصفية فورية**: أزرار سريعة أعلى القائمة لعرض الكل أو النصوص أو الروابط أو
  الصور أو المثبّت فقط.
- **بحث أوضح**: حقل بحث جديد مع زر لمسح ما كتبته، ونتائج تظهر أثناء الكتابة.
- **اختصارات سياقية وإجراءات الصف**: يعرض أسفل اللوحة الاختصارات المتاحة فقط؛
  ويمكن تثبيت كل عنصر أو حذفه مباشرة، مع Toast عربي وتراجع آمن لمدة خمس ثوانٍ.
- **إعدادات أبسط**: نافذة واحدة منظّمة في خمس صفحات واضحة يمكن التنقل بينها
  بالماوس أو لوحة المفاتيح.
- **نافذة «عن رفّ» مستقلة**: تعرض الإصدار الحالي ورابط المستودع.
- **مظهر فاتح وداكن منسجم**: ألوان وخطوط موحّدة في الوضعين.
- **هوية إنتاجية واحدة**: أيقونة تطبيق معتمدة واحدة بدل متغيرات المظهر
  القديمة؛ حقول الإعدادات التاريخية غير المعروفة تُقبل عند قراءة ملف قديم
  ثم تُزال بأمان عند الحفظ التالي.
- **حمولة واجهة محكومة**: يبني Tauri مجلد `dist/` من قائمة مسموحة صريحة، فلا
  تدخل بيانات المحاكاة أو صادرات صفحات Figma أو الملفات الخفية في الحزمة.
- لا تغيير على صيغة بيانات الحافظة أو مكانها؛ أضيفت اختصارات الصف المعلنة
  `⌥P` و`⌘⌫` و`⌘Z` إلى مسار التفاعل الحالي.
- **أيقونات أخف وأكثر اتساقًا**: أُعيد ضبط سماكة الحدود لكل عائلة أيقونات
  (إجراءات الصف، الأدوات، أيقونات المصدر) على حدة بدل قيمة واحدة موحّدة،
  فعادت العناصر التفاعلية أثقل بصريًا من العناصر الفرعية كما يجب، دون أن تبدو
  ثقيلة أو غير متناسقة كما كانت.
- **محاذاة أدق للوحة**: حقل البحث والقائمة وشريط الاختصارات السفلي تتقاسم
  الآن هامشًا داخليًا واحدًا متطابقًا على كامل عرض اللوحة.
- **أيقونة تطبيق مصقولة**: بُنيت الأيقونة من جديد بزوايا مستمرة (Squircle)
  وعمق بصري أقرب لمعايير macOS الحديثة، مع الحفاظ الكامل على هوية رفّ
  البرتقالية الدافئة.

## سجل الإصدارات المحفوظ

ما يلي سجل تاريخي للإصدارات السابقة فقط. قد ترد فيه أسماء أصول أو علامات أو
ألوان أو مقاسات انتهى استخدامها؛ هي اقتباسات وصفية مؤرخة وليست تعليمات
تصميم أو ملفات شحن. أصولها غير موجودة في المصدر أو الحزمة الحالية.

## v3.1.0 — 2026-08-11

تنقيح نهائي على شعار رفّ الجديد الذي قُدِّم في v3.0.0.

- **الشعار وأيقونة التطبيق نُقِّحا مرة أخيرة**: رمز أكثر تميّزًا ووضوحًا في
  الأحجام الصغيرة — قوس حاضن يحيط بعنصرين، يعبّر عن فكرة الحفظ والاحتواء
  التي يقوم عليها رفّ دون تشبيه حرفي بأي شيء آخر. يستبدل شعار v3.0.0 في كل
  مكان: أيقونة التطبيق، شريط القائمة، لوحة رفّ، ونافذتا الإعدادات وحول.
- تحسينات طفيفة في الأيقونات التوضيحية لحالات «الرفّ فارغ» و«تعذّر العرض»
  و«لا نتائج» لتنسجم مع الشكل النهائي للشعار.
- لا تغيير على البيانات المحفوظة، ولا على آلية تسجيل عناصر الحافظة، ولا على
  الاختصارات أو الإعدادات أو مكان بياناتك.

## v3.0.0 — 2026-08-11

هوية بصرية جديدة كليًا لرفّ، من الشعار إلى أدق تفاصيل الواجهة.

- **شعار جديد**: رمز هندسي جديد يجمع مسارًا دائريًا مع خط أفقي ونقطة تثبيت
  مركزية — يستبدل الرمز السابق في كل مكان: أيقونة التطبيق، شريط القائمة،
  ونافذتا الإعدادات وحول.
- **أيقونة تطبيق جديدة بالكامل**، بتدرّج لوني دافئ في الوضع النهاري وتدرّج
  داكن غنيّ في الوضع الليلي، بأحجام مضبوطة لكل سياق — من شريط القائمة إلى
  Finder وDock.
- **تحسينات في الألوان والتباين** عبر الواجهة كاملة: حدود وحقول ونصوص ثانوية
  أكثر دفئًا واتساقًا، وشارة «محلي» صارت خضراء لتعبّر عن الخصوصية بوضوح أكبر.
- **أزرار وحقول مُعاد تصميمها**: حالات أوضح عند التمرير والضغط والتركيز في
  كل مكان تقريبًا، وأزرار الحذف والمسح صارت حمراء صريحة بدل التلميح الخافت.
- **تحسينات صغيرة في الأيقونات التوضيحية** لحالتَي «الرفّ فارغ» و«لا نتائج»
  لتنسجم مع الهوية الجديدة.
- لا تغيير على البيانات المحفوظة، ولا على آلية تسجيل عناصر الحافظة، ولا على
  الاختصارات أو الإعدادات أو مكان بياناتك.

## v2.1.4 — 2026-07-22

تحسين استقرار واجهة رَفّ عند إعادة فتح النافذة بعد بقائها مخفية أو بعد
استئناف الجهاز من السكون.

- **معالجة ظهور النافذة فارغة**: نافذة رَفّ تبقى حيّة طوال الجلسة ولا تُغلق،
  فيعلّقها النظام حين تختفي. السبب الأرجح والمدعوم بسجلات WebKit هو تأخر أو
  غياب إعادة رسم طبقات الواجهة بعد استئناف النافذة، وقد عولج بإجبار إعادة
  الرسم عند كل إظهار وعند استعادة التركيز.
- **مسار تعافٍ تلقائي**: إن تعذّر تحميل العناصر، يعاد المحاولة مرتين بفاصل
  قصير قبل إظهار أي رسالة. وإن نجح تحميل سابق، تبقى آخر قائمة سليمة معروضة
  بدل تفريغ الواجهة عند خلل عابر.
- **زر «تحديث رَفّ»**: زر صغير في الشريط العلوي بجوار شارة «محلي». يعيد تحميل
  البيانات ورسم الواجهة في مكانها أولًا، ولا يلجأ لإعادة تحميل كاملة إلا عند
  تعذّر ذلك. الضغط المتكرر أثناء التنفيذ ممنوع.
- **حالة تعافٍ واضحة بدل الفراغ الصامت**: عند تعذّر العرض تظهر رسالة عربية
  «تعذّر عرض محتوى رَفّ» مع زر «إعادة تحميل الواجهة». حالات «جارٍ التحميل»
  و«رفّك فارغ» و«تعذّر عرض المحتوى» صارت منفصلة، فلا تُعرض حالة الرفّ الفارغ
  عند وجود خطأ.
- **قائمة النقر بالزر الأيمن**: لم تعد قائمة WebView الإنجليزية تظهر فوق
  المساحات غير التفاعلية. النسخ واللصق والتحديد داخل حقل البحث تعمل كما هي.
- لا تغيير على البيانات المحفوظة، ولا على آلية تسجيل عناصر الحافظة، ولا على
  قواعد منع التكرار أو الإعدادات أو مكان بياناتك.

## v2.1.3 — 2026-07-21

نقل قناة التحديث الرسمية إلى مستودع «رفّ» نفسه. لا تغييرات وظيفية.

- **قناة التحديث انتقلت من `Raff-releases` إلى مستودع `Raff`**: صار المستودع
  الواحد هو مصدر المشروع الرسمي — الكود والوثائق والإصدارات وحِزم التحديث
  في مكان واحد بدل مستودعين منفصلين. النقطة التي يسألها التطبيق صارت
  `https://github.com/iSltanX/Raff/releases/latest/download/latest.json`.
- كل الإصدارات السابقة (v1.3.1 → v2.1.2) نُقلت إلى المستودع الجديد بملفاتها
  وتواقيعها كما هي، بلا إعادة بناء ولا إعادة توقيع؛ بصمات الحزم مطابقة
  للأصل بايتًا ببايت.
- **مفتاح التوقيع لم يتغيّر**. الحزم ما زالت موقّعة بالمفتاح نفسه، والتطبيق
  يتحقق منها بالمفتاح العام نفسه المدمج فيه منذ v1.3.1 — أي نسخة مثبَّتة
  تقبل هذا التحديث دون أي إجراء منك.
- **هذا الإصدار هو جسر الانتقال**: النسخ المثبَّتة حتى v2.1.2 تحمل عنوان
  القناة القديمة داخلها، فلا تستطيع وحدها معرفة العنوان الجديد. لذلك يُعلَن
  عن v2.1.3 على القناة القديمة أيضًا، وبتثبيتها ينتقل التطبيق إلى القناة
  الجديدة تلقائيًا وتصير كل التحديثات اللاحقة منها. القناة القديمة تبقى
  قائمة للنسخ التي لم تنتقل بعد.
- لا تغيير في اسم التطبيق أو معرّفه أو اختصاراته أو إعداداته أو مكان بياناتك
  أو أي سلوك في الواجهة. الترقية من v2.1.2 لا تمسّ سجل الحافظة ولا العناصر
  المثبّتة ولا الإعدادات.
- إضافة تدفّق نشر آلي (GitHub Actions) يبني الحزم ويوقّعها وينشرها في
  إصدارات المستودع الرئيسي عند دفع وسم جديد.

## v2.1.2 — 2026-07-21

إصلاح خلل وظيفي، وإضافة خيار لمسح سجل الحافظة.

- **إصلاح نافذة «رفّ» الفارغة بعد نسخ عنصر قديم من السجل**: بعد اختيار عنصر
  ونسخه/لصقه، إخفاء النافذة (سلوك متوقّع) ثم إعادة فتحها كانت تعرض قائمة
  فارغة تمامًا رغم بقاء البيانات سليمة على القرص — ولا تعود العناصر للظهور
  إلا بعد إنهاء التطبيق بالكامل وإعادة تشغيله.
- **السبب**: اللوحة العائمة webview واحدة تعيش طوال جلسة التطبيق (لا تُعاد
  ولا تُبنى من جديد عند كل إظهار)، وكانت مزامنة حالتها مع المخزون تعتمد فقط
  على حدث `panel://shown` القادم من Rust. عند استعادة تركيز النافذة دون وصول
  هذا الحدث تحديدًا، لا يوجد أي مسار آخر يعيد جلب السجل.
- **الإصلاح**: أضيفت مزامنة إضافية عند استعادة تركيز النافذة (`window focus`)
  تمر عبر نفس مسار التحديث الرسمي `refresh()`، إلى جانب حارس تسلسلي
  (`refreshToken`) يمنع استجابة بطيئة أو فاشلة من IPC من الكتابة فوق حالة
  أحدث منها أو من تجميد الواجهة. لا مستمعِين مكرَّرين، ولا إعادة تحميل كاملة
  للصفحة، ولا تغيير في سلوك النسخ/اللصق أو التصميم.
- أُضيفت مجموعة اختبارات انحدار (`tests/panel.test.mjs`) تُشغِّل كود اللوحة
  الفعلي عبر جسر Tauri وهمي يحاكي توقيت IPC الحقيقي: عناصر نصية وصور
  ومثبَّتة، تكرار الإخفاء/الإظهار ١٠ مرات، تعارض الطلبات غير المتزامنة،
  فشل IPC مؤقت، وحالة البحث — كلها دون فقدان بيانات أو تكرار عناصر.

- **خيار «مسح سجل الحافظة» في الإعدادات** (تبويب الخصوصية ← البيانات): كان
  الزر سابقًا «مسح السجل كاملًا» بتأكيد من ضغطتين على الزر نفسه — سهل التنفيذ
  بالخطأ لعملية حذف لا رجعة فيها. صار الآن يفتح نافذة تأكيد صريحة تذكر
  العواقب («سيتم حذف جميع العناصر غير المثبّتة… لا يمكن التراجع») بزرَّي
  «إلغاء» و«مسح السجل»، مع تركيز أوّلي على «إلغاء» حتى لا يؤدي ضغط Return
  عابر إلى حذف. يُلغى التأكيد بـ Escape أو بالنقر خارج النافذة.
- تُحذف عناصر السجل غير المثبّتة فقط؛ **العناصر المثبّتة تبقى كما هي**، وكذلك
  الإعدادات والاختصار العام والمظهر وبيانات التطبيق الأخرى. تُحدَّث الواجهة
  فورًا دون إعادة تشغيل، وتظهر رسالة تأكيد هادئة «تم مسح سجل الحافظة.».
- تغطية اختبارية للمسار: رفض التأكيد (زر الإلغاء وEscape والنقر خارج النافذة)
  لا يحذف شيئًا، وقبوله يحذف غير المثبّت فقط
  (`tests/settings-clear-history.test.mjs` + اختبارَي `clear_history_keeps_pinned`
  و`clear_history_leaves_settings_and_pinned_signals_alone` في Rust).

## v2.1.1 — 2026-07-19

إصلاح عام.

- **إصلاح تشوّه توقيع «برمجة وتصميم سلطان»**: إصلاح v2.1.0 استبدل التوقيع
  بنسخة SVG ذات نص حي، مع حزمة خط فرعية (subset) مضمَّنة لضمان ظهور
  Almarai/Cairo بدل الخط الاحتياطي. لكن استخراج الحزمة الفرعية أسقط جداول
  الربط الحرفي العربي (GSUB) اللازمة لأشكال الحروف السياقية، فظهرت الحروف
  منفصلة ومشوَّهة بدل اتصالها الطبيعي.
- التصحيح: التوقيع صار صورة نقطية (PNG) مُصيَّرة بدقة ×6 بالخط الكامل غير
  المجزَّأ، فلا يعتمد إطلاقًا على وجود أي خط وقت العرض — لا يمكن أن يتكرر
  هذا العطل. نفس الحدود المقصوصة والألوان والنِّسبة والمقياس على الشاشة
  كما في v2.1.0، بلا أي تغيير تصميمي.
- الأصل الرسمي نفسه (هوية سلطان) لم يُعدَّل ولم يُعَد رسمه؛ هذا تصحيح في
  طريقة تسليم الأصل فقط.

## v2.1.0 — 2026-07-19

إصلاحات عامة.

- **تحسين تنفيذ توقيع «برمجة وتصميم سلطان»** في نافذة «حول»:
  - **الخط**: التوقيع كان يُعرَض عبر `<img>` يشير إلى ملف SVG خارجي؛ داخل
    عنصر `<img>` لا يستطيع المتصفح الوصول إلى خطوط `@font-face` المعرَّفة في
    صفحة التطبيق، فكان يسقط تلقائيًا إلى Arial بدل Almarai/Cairo الرسميين.
    أُضيف للملف نفسه حزمة خط فرعية (subset) محمَّلة ذاتيًا تقتصر على
    الحروف المستخدمة فعليًا في نص التوقيع، فيعرض الخط الصحيح دائمًا.
  - **التمركز**: مساحة العمل (viewBox) الأصلية للأصل كانت أوسع من محتواه
    الفعلي بنحو 44%، فكانت كتلة الأيقونة والنص تظهر منزاحة عن المركز رغم
    محاذاة الحاوية. جرى قصّ الفراغ الميت المحيط بالمحتوى فقط (بهامش موحّد
    على الجهات الأربع)، دون أي تغيير في المسارات أو النص أو الألوان أو
    النسبة أو المقياس الظاهر على الشاشة.
  - الأصل الرسمي (`assets/rights/rights-bilingual.svg` من هوية سلطان) لم
    يتغيّر تصميمًا أو خطًا أو لونًا أو نسبة؛ التصحيح تقني بحت في طريقة
    تحميل الخط وحدود إطار العرض.
- إصلاحات وتحسينات عامة على الاستقرار والتناسق البصري.

## v2.0.0 — 2026-07-19

إصدار رئيسي: اعتماد نظام هوية «رفّ» البصري v2 على كامل واجهة التطبيق.

### الهوية البصرية

- اعتماد نظام الهوية `RAFF-Identity-System-v2` مصدرًا وحيدًا للتصميم.
- استبدال طبقة الألوان بالكامل بـ Semantic Design Tokens الرسمية
  (`raff.tokens.json` / `tokens.ts`)، مع علاقة صحيحة ومستقلة بين الوضعين
  الفاتح والداكن بدل اشتقاق أحدهما من الآخر.
- تصحيح دوري الخطّين وفق دليل الهوية: **Almarai 800/700** للعناوين والعلامة
  وعناصر التحكم، و**Cairo 500/400** للمحتوى والحقول والرسائل.
- اعتماد مقاييس النظام الرسمية: Spacing وRadius وBorders وShadows وBlur
  وMotion (المدد ومنحنيات التسارع).
- اعتماد علامة هندسية تجريبية في شريط عنوان اللوحة آنذاك.

### الأيقونات

- استبدال أيقونة التطبيق بالأصول الرسمية: `Raff.iconset` تُجمَّع مباشرة إلى
  `icon.icns` دون إعادة رسم أو إعادة تنقيط.
- توليد نسخة تطبيق داكنة تاريخية بكل مقاسات Retina.
- استبدال أيقونة شريط القائمة آنذاك بقالب أحادي اللون ذي قناة شفافية.

### اللوحة العائمة

- اعتماد عرض اللوحة الرسمي 392px ونصف قطر 18px وBlur 28px.
- شريط عنوان يحمل علامة «رفّ» ومؤشّر «محلي».
- حقل بحث بارتفاع 34px بحلقة تركيز 2px وفق نموذج التركيز المعتمد.
- صفوف الحافظة بارتفاع 54px مع شارة نوع 28×28، وحالة تحديد بسكة جانبية
  وحدّ بلون العلامة.
- حالات Hover وPressed وFocus وحالات القائمة الفارغة وفق النظام.

### نافذة «حول»

- إعادة بناء النافذة بأيقونة «رفّ» الرسمية والاسم العربي واللاتيني.
- الحفاظ الكامل على قسم التحديثات وزر التحقق وجميع حالاته.
- إضافة توقيع الحقوق «برمجة وتصميم: سلطان» من الأصل الرسمي في هوية سلطان،
  كخاتمة للنافذة، بنسختيه الفاتحة والداكنة الرسميتين.

### الإعدادات

- اعتماد عرض النافذة الرسمي 720px.
- تحديث بطاقات معاينة المظهر لتعرض قيم الوضعين الفعلية الجديدة.

### إصلاحات تقنية

- تصحيح أوزان الخطوط في العناوين: كانت تطلب وزن 600 غير الموجود في Almarai
  فيُصطنَع اصطناعيًا؛ صارت تستخدم 700/800 الحقيقيين.
- تصحيح ترميز XML في أصل توقيع الحقوق: محرف `&` غير المهرَّب كان يجعل ملف
  SVG غير صالح فلا يُعرض داخل `<img>`؛ صار `&amp;` دون أي أثر بصري.
- توحيد تنسيق مصادر Rust عبر `cargo fmt`.

### التوثيق

- إعادة بناء `README.md` بأصول GitHub الرسمية ولقطات من هذا الإصدار.
- إضافة هذا السجل.

### بلا تغيير

- نموذج الخصوصية: محلي بالكامل، بلا شبكة ولا حسابات ولا تتبّع.
- بنية التخزين وبيانات المستخدم واختصاراته المحفوظة.
- قناة التحديث ومفاتيح التوقيع.

{
  "name": "raff",
  "version": "3.0.0",
  "private": true,
  "description": "رفّ — personal, local-only, keyboard-first clipboard manager for macOS",
  "type": "module",
  "scripts": {
    "tauri": "tauri",
    "test": "node --test --test-force-exit tests/*.test.mjs"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2",
    "jsdom": "^29.1.1"
  }
}

{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "designed-chrome",
  "description": "Settings and About use custom title bars, so these two windows may close themselves and start native title-bar drags.",
  "windows": ["settings", "about"],
  "permissions": ["core:window:allow-close", "core:window:allow-start-dragging"]
}
